# Example environment variables
# Copy to .env and set values as needed.

# App
APP_ENV=dev

# Data directories (can leave as default)
DATA_DIR=./data
RAW_DIR=./data/raw
EXTERNAL_DIR=./data/external
PROCESSED_DIR=./data/processed

# Backend server
API_HOST=0.0.0.0
API_PORT=8000

# (Later) Database connections
# POSTGRES_URL=postgresql://user:pass@localhost:5432/drugs
# REDIS_URL=redis://localhost:6379/0
