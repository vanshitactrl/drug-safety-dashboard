# Minimal packages for Step 0 demo and later steps
pandas>=2.0
numpy>=1.26
requests>=2.31
beautifulsoup4>=4.12

# Web / UI
streamlit>=1.33
uvicorn>=0.30
fastapi>=0.111

# Config
python-dotenv>=1.0
pydantic>=2.7
PyYAML>=6.0

# (Later) Data / DB / Cache (we will pin when used)
SQLAlchemy>=2.0
psycopg2-binary>=2.9
redis>=5.0

# (Later) NLP
spacy>=3.7

# Dev
pytest>=8.0
